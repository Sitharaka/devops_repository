Backend instructions:

1. cd DevOPs/backend
2. npm install
3. Set environment variables if needed:
   - MONGO_URI (optional)
   - PORT (optional)
4. Start server: npm start or node app.js

This adds required dependencies bcryptjs and jsonwebtoken for authentication.